web:
  build: .
  environment:
    - AWS_ACCESS_KEY
    - AWS_SECRET_KEY
    - PYTHONPATH=.
    - DJANGO_SETTINGS_MODULE=webscreenshots.settings.fig
  command: python manage.py runserver 0.0.0.0:8000
  volumes:
    - .:/code/
  ports:
    - "8000"
  links:
    - db
#dbdata:
#  image: postgres:latest
#  volumes:
#    - /var/lib/postgresql
#  command: true
db:
  image: postgres:latest
#  volumes_from:
#    - dbdata
  ports:
    - "5432"
  environment:
    - USER=postgres
    - DB=postgres
